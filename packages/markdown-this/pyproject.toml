[project]
name = "markdown-this"
version = "0.1.12"
description = "Extract web pages and supported special URLs as Markdown"
readme = "README.md"
authors = [{ name = "Martín Gaitán", email = "gaitan@gmail.com" }]
license = { text = "MIT" }
requires-python = ">=3.12"
keywords = ["html", "markdown", "readability", "github", "arxiv"]
dependencies = [
    "beautifulsoup4>=4.14.3",
    "markdownify>=1.2.2",
    "pyyaml>=6.0.3",
    "readability-lxml>=0.8.4.1",
    "requests>=2.32.5",
    "youtube-transcript-api>=1.2.4",
]

[project.urls]
Homepage = "https://github.com/mgaitan/markdown-tools/tree/master/packages/markdown-this"
Repository = "https://github.com/mgaitan/markdown-tools"
Issues = "https://github.com/mgaitan/markdown-tools/issues"

[project.scripts]
markdown-this = "markdown_this.cli:main"

[build-system]
requires = ["uv_build>=0.7,<0.12"]
build-backend = "uv_build"
